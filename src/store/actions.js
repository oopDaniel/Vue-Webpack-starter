// import * as types from './mutation-types'

export default {
  // [types.ADD_ONE]: ({ commit }) => {
  //   commit(types.ADD_ONE)
  // },
  // asyncAddOne: ({ commit }, delay = 3000) => {
  //   setTimeout(() => commit(types.ADD_ONE), delay)
  // }
}
